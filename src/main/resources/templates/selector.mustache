<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Discord Bot Initializr</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/selector-styles.css">
</head>
<body class="bg-discord-dark">
<div class="container mx-auto px-4 py-12 max-w-6xl">
    <header class="text-center mb-12">
        <div class="title-container mb-6">
            <h1 class="text-5xl font-black text-transparent bg-clip-text bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 mb-2">Discord Bot Initializr</h1>
            <div class="w-64 h-1 mx-auto bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 rounded-full"></div>
        </div>
        <p class="text-xl text-gray-300 font-light">Craft your perfect Discord bot with powerful modules</p>
    </header>

    <main class="relative flex flex-col space-y-8">
        <div class="grid grid-cols-2 gap-8">
            <!-- 왼쪽 컨테이너: 선택되지 않은 항목 -->
            <div class="main-container p-6 min-h-[400px]">
                <h2 class="text-2xl font-bold text-gray-200 mb-4 flex items-center">
                    <span class="w-2 h-8 bg-indigo-500 rounded-full mr-3"></span>
                    Available Modules
                </h2>
                <div id="unselectedContainer" class="space-y-4">
                    {{#cogsFileNames}}
                        <div class="file-item-transition p-5" data-item-id="{{.}}">
                            <div class="flex items-center space-x-4">
                                <label class="flex items-center space-x-4 cursor-pointer w-full">
                                    <input type="checkbox"
                                           class="checkbox-custom"
                                           id="{{.}}"
                                           name="cogsFiles"
                                           value="{{.}}"
                                           onchange="moveItem(this)">
                                    <div class="flex flex-col flex-grow">
                                        <span class="file-name">{{.}}</span>
                                        <span class="file-description mt-1">Python module for {{.}}</span>
                                    </div>
                                </label>
                            </div>
                        </div>
                    {{/cogsFileNames}}
                </div>
            </div>

            <!-- 오른쪽 컨테이너: 선택된 항목 -->
            <div class="main-container p-6 min-h-[400px]">
                <h2 class="text-2xl font-bold text-gray-200 mb-4 flex items-center">
                    <span class="w-2 h-8 bg-purple-500 rounded-full mr-3"></span>
                    Selected Modules
                </h2>
                <div id="selectedContainer" class="space-y-4">
                    <!-- 선택된 항목들이 여기로 이동됨 -->
                </div>
            </div>
        </div>

        <!-- 중앙 버튼 컨테이너 -->
        <div class="flex flex-col items-center space-y-6 mt-8">
            <button id="processButton"
                    class="button-primary group"
                    onclick="processSelectedFiles()">
                <span>Generate Discord Bot</span>
                <span id="loadingSpinner" class="loading-spinner ml-3 hidden"></span>
            </button>

            <div id="errorMessage" class="hidden w-full max-w-xl p-4" role="alert">
            </div>
        </div>
    </main>
</div>

<script src="/js/selector-script.js"></script>
</body>
</html>