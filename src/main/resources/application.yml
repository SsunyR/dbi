spring:
  application:
    name: discord-bot-initializer
  mustache:
    servlet:
      cache: false
  web:
    resources:
      add-mappings: true

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always


logging:
  level:
    root: INFO
    org.springframework: INFO
    Grad_Project.Discord_Bot_Initializer.controller: DEBUG